package Packet;

    
message PacketHeader {
  
  required int32 packet_type = 1;
  optional LoginPacket login_packet = 2;

  message LoginPacket {
      required string username = 2;
      required string password = 3; 
      optional int32 version  = 4;
  }

}
